%header.site-header
  = link_to root_path, class: 'brand-logo' do
    = image_tag "kazimir_logo.png", alt: "KAZIMIR"


  %nav.nav-lang
    - if params[:locale] == 'en'
      = link_to "Wersja polska", '/pl'
    - else
      = link_to "English version", '/en'
