%header.site-header
  = link_to root_path, class: 'brand-logo' do
    = image_tag "kazimir_logo.png", alt: "KAZIMIR"

  %nav.nav-lang
    - if params[:locale] == 'en'
      = link_to "PL", '/pl'
    - else
      = link_to "EN", '/en'
