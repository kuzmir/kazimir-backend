%table.table
  %thead
    %tr
      %th #
      %th.col-md-6
        - if local_assigns.has_key? :headline
          = headline
        - else
          Place name
      %th Photos
      %th Actions
  %tbody
    - if places.size > 0
      - places.each_with_index do |place, index|
        %tr
          %td= index + 1
          %td= link_to place.name, admin_street_place_path(place.street, place)
          %td= place.photos.size
          %td
            = link_to 'show', admin_street_place_path(place.street, place), class: 'btn btn-default btn-sm'
            = link_to 'edit', edit_admin_street_place_path(place.street, place), class: 'btn btn-default btn-sm'
            = link_to 'delete', admin_street_place_path(place.street, place), class: 'btn btn-warning btn-sm', method: :delete, data: {confirm: "Are you sure to delete?"}
    - else
      %tr
        %td{colspan: 3} No places added yet.
